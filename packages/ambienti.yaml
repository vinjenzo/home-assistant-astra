# ============================================================
# AMBIENTI
# Ogni scena applica brightness definiti in scene.yaml
# ============================================================

script:
  # ========================================
  # HOUSE SCENES
  # ========================================
  scene_casa_notte:
    alias: "Ambiente Casa di Notte"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "casa_notte"
      # Spegni TV
      - service: media_player.turn_off
        target:
          entity_id: media_player.lg_webos_tv_oled48c31la

  scene_casa_luminosa:
    alias: "Ambiente Casa Luminosa"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "casa_luminosa"

  scene_casa_ospiti:
    alias: "Ambiente Casa Ospiti"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "casa_ospiti"

  scene_casa_pulizie:
    alias: "Ambiente Casa Pulizie"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "casa_pulizie"

  scene_casa_spenta:
    alias: "Spegni tutte le luci di casa"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "casa_spenta"

  # ========================================
  # SOGGIORNO SCENES
  # ========================================
  scene_soggiorno_luminoso:
    alias: "Ambiente Soggiorno Luminoso"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "soggiorno_luminoso"

  scene_soggiorno_risveglio:
    alias: "Ambiente Soggiorno Risveglio"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "soggiorno_risveglio"

  scene_soggiorno_bilanciato:
    alias: "Ambiente Soggiorno Bilanciato"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "soggiorno_bilanciato"

  scene_soggiorno_relax:
    alias: "Ambiente Soggiorno Relax"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "soggiorno_relax"

  scene_soggiorno_sonnellino:
    alias: "Ambiente Sonnellino"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "soggiorno_sonnellino"

  scene_soggiorno_sonno_notturno:
    alias: "Ambiente Sonno Notturno"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "soggiorno_sonno_notturno"

  scene_soggiorno_lettura:
    alias: "Ambiente Soggiorno Lettura"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "soggiorno_lettura"

  scene_soggiorno_lavoro:
    alias: "Ambiente Soggiorno Lavoro"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "soggiorno_lavoro"

  scene_soggiorno_notte:
    alias: "Ambiente Soggiorno Notte"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "soggiorno_notte"
      # Spegni TV
      - service: media_player.turn_off
        target:
          entity_id: media_player.lg_webos_tv_oled48c31la

  scene_soggiorno_spento:
    alias: "Spegni tutte le luci del Soggiorno"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "soggiorno_spento"

  scene_tv_calcio:
    alias: "Ambiente Calcio"
    mode: queued
    sequence:
      - service: script.tv_calcio
      - service: script.set_ambiente
        data:
          scene_key: "tv_calcio"

  scene_tv_cinema:
    alias: "Ambiente Cinema"
    mode: queued
    sequence:
      - service: script.tv_cinema
      - service: script.set_ambiente
        data:
          scene_key: "tv_cinema"

  scene_tv_computer:
    alias: "Ambiente Computer"
    mode: queued
    sequence:
      - service: script.tv_computer_hdmi2
      - service: script.set_ambiente
        data:
          scene_key: "tv_computer"

  # ========================================
  # KITCHEN SCENES
  # ========================================
  scene_cucina_luminosa:
    alias: "Ambiente Cucina Luminosa"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "cucina_luminosa"

  scene_cucina_lavoro:
    alias: "Ambiente Cucina Preparazione Cibo"
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "cucina_lavoro"

  scene_cucina_cena:
    alias: "Ambiente Cena in Cucina"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "cucina_cena"

  scene_cucina_relax:
    alias: "Ambiente Cucina Relax"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "cucina_relax"

  scene_cucina_notte:
    alias: "Ambiente Cucina Notte"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "cucina_notte"

  scene_cucina_spenta:
    alias: "Spegni tutte le luci della Cucina"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "cucina_spenta"

  # ========================================
  # CAMERETTA SCENES
  # ========================================
  scene_cameretta_luminosa:
    alias: "Ambiente Cameretta Luminosa"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "cameretta_luminosa"

  scene_cameretta_relax:
    alias: "Ambiente Cameretta Relax"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "cameretta_relax"

  scene_cameretta_notte:
    alias: "Ambiente Cameretta Notte"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "cameretta_notte"

  scene_cameretta_lavoro:
    alias: "Ambiente Cameretta Lavoro"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "cameretta_lavoro"

  scene_cameretta_spenta:
    alias: "Spegni tutte le luci della Cameretta"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "cameretta_spenta"

  # ========================================
  # BEDROOM SCENES
  # ========================================
  scene_camera_da_letto_luminosa:
    alias: "Ambiente Camera da letto Luminosa"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "camera_da_letto_luminosa"

  scene_camera_da_letto_sera:
    alias: "Ambiente Camera da letto Sera"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "camera_da_letto_sera"

  scene_camera_da_letto_lettura:
    alias: "Ambiente Camera da letto Lettura"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "camera_da_letto_lettura"

  scene_camera_da_letto_relax:
    alias: "Ambiente Camera da letto Relax"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "camera_da_letto_relax"

  scene_camera_da_letto_notte:
    alias: "Ambiente Camera da letto Notte"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "camera_da_letto_notte"

  scene_camera_da_letto_spenta:
    alias: "Spegni tutte le luci della Camera da letto"
    mode: queued
    sequence:
      - service: script.set_ambiente
        data:
          scene_key: "camera_da_letto_spenta"
